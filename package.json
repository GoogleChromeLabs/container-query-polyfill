{
  "name": "cqfill",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "build:minified": "esbuild --bundle ./cqfill.ts --outfile=cqfill.min.js --format=esm --minify",
    "build:debug": "esbuild --bundle ./cqfill.ts --outfile=cqfill.js --format=esm",
    "build": "npm run build:debug && npm run build:minified"
  },
  "author": "Surma <surma@surma.dev>",
  "license": "Apache-2.0",
  "dependencies": {
    "esbuild": "^0.13.13"
  }
}
